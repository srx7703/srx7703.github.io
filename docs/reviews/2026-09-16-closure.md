# Closure review (2026-09-16)

Reviewer: read-only Claude Code subagent. Verified on HEAD `80f8e4a`. Tests: `uv run pytest -q -p no:warnings` → 19 passed. Lint: `uv run ruff check pipelines` → all checks passed.

| Finding | Verdict | Evidence |
|---|---|---|
| P1/S1 deploy after data commits | CLOSED | `deploy-site.yml` `workflow_run` on both data workflows. Proven live: deploy run event=`workflow_run` on `e45e9ba`; `/projects/fomc-markets/` shows "Data as of Sep 16, 2026, 06:37 UTC · 4 snapshots". |
| P2 FOMC scorecard can never populate | CLOSED | `snapshot.py` `capture_resolutions` (Kalshi settled sweep + Polymarket closed 1/0) on full runs; `fomc.py` `resolutions()` reads the store; 68 markets recorded by CI in `e45e9ba`. No unit test covers it. |
| P3 publish failure discards snapshot | CLOSED | `continue-on-error` on publish, `if: always()` on commit in both workflows. |
| P4 markets_tracked flips with scope | CLOSED (midterms) | coverage filtered to full runs. Residual: `fomc.py` `markets_tracked` still from the latest snapshot (not rendered). |
| P5 "live markets" inflated | CLOSED | quoted = both sides ∧ spread ≤ 0.10 ∧ volume > 0; page wording updated. |
| P6 Brier cutoff / normalisation | CLOSED | timestamp cutoff on snapshot rows; `sum_prob`; range and non-normalisation stated on the page and in `EVALUATION_PLAN.md`. Minor: scorecard table lacks a sum column. |
| P7 SEC_USER_AGENT | CLOSED | `os.environ.get(...) or default`; `companies.json` no longer carries the UA. |
| P8/S3/S8 hand-typed numbers | PARTIAL | attributes and 2-digit numbers scanned; result allow-list entries removed. Gaps: frontmatter `summary` still hand-types results (statarb "85 large caps", finllm "1,060 … 381 … 3.5% and 5.8%"), rendered on cards and meta descriptions; `how-i-work` "47 findings" outside the scan. |
| P10 push retry vs rebase conflict | CLOSED | `git rebase --abort` + `git pull --rebase -X theirs`. |
| P14 retired Kalshi series aborts run | CLOSED | `_safe_series` used in all three loops. |
| S2 no test gate | CLOSED | pytest step before Build; latest deploy run shows it succeeded. |
| S3 false hero title | CLOSED | title from `facts.zero_cost` / `sharpe_drop_per_10bps`; live h2 "No static model is profitable even at zero cost; each 10 bps … removes about 0.7 of Sharpe". |
| S4 lede wrong side | CLOSED | `modal_side/modal_prob/agree` from facts; live text matches facts. |
| S6 "+5.8 points" | CLOSED (body) | `%` from `deltas_pct`; points from `facts.f1_points`. Residual = the frontmatter "5.8%". |
| S7 truncated bars | CLOSED | `domain: [0, 1]`, value labels. |
| S11 scatter line / domains | CLOSED | data-driven domains, endpoints on domain edges, `clip: true` on all layers. |
| S12 path title | CLOSED | `pathTitle` from `facts.meetings`. |
| S13 facet mobile | CLOSED | `VegaChart.astro` sizes facet panels from container width ÷ `_facetColumns`. |
| S16 check counts | CLOSED | `DataQuality.astro` on all five pages; live shows "N of M checks passed". |
| S17 pre-reg anchor | CLOSED | `docs/EVALUATION_PLAN.md`; `git hash-object` = `24a3e53d0919…`; printed on both live pages. |
| S18 heading order | CLOSED (residual) | ChartCard h2. Residual: `/projects/` h1→h3. |
| S25 th role=button | CLOSED | removed. |
| S27/S28 How-I-work | PARTIAL | rewritten with reviewers, gates, failure log. Unsupported claims: "a failing check blocks the write" (only pandera schemas block); two failure-log entries not visible in git history (fixed before first commit); "47 findings" unverifiable in repo; PNG figure lacks a SourceStamp. |

## Still open after this review (all fixed in the next commit)

1. Frontmatter summaries hand-type result numbers.
2. How-I-work overclaims (blocking checks; failure-log entries predating the first commit; unverifiable count).
3. SaaS page: "Coverage" sentence (any-quarter) contradicts the Data-quality table (latest-quarter nulls).
4. `fomc.json` `markets_tracked` scope-dependent (latent).
5. `/projects/` index skips h1→h3.
6. No tests for `capture_resolutions`, `_safe_series`, `full_run_timestamps`, `resolutions()`.
7. Minor: scorecard lacks the `sum_prob` column; deltas precision "3.5%" vs "5.76%".
